# Copy this to .env and fill in your values

# =============================================================================
# Required: API Configuration (security-sensitive, env-only)
# =============================================================================

# API key (for OpenRouter, or your chosen provider)
API_KEY=your_api_key_here

# Provider configuration (defaults to OpenRouter)
PROVIDER_TYPE=openrouter
BASE_URL=https://openrouter.ai/api/v1

# =============================================================================
# Optional: CLI defaults (can be overridden via command-line arguments)
# =============================================================================

# Model to use (OpenRouter format: provider/model-name)
# Can be overridden with --model flag
# Examples:
#   - google/gemini-2.5-flash-lite (fast & cheap)
#   - anthropic/claude-3-haiku
#   - anthropic/claude-3.5-sonnet
#   - meta-llama/llama-3.1-8b-instruct
#   - openai/gpt-4-turbo
MODEL_NAME=google/gemini-2.5-flash-lite

# Random seed for reproducibility (default: 0)
# Can be overridden with --seed flag
RANDOM_SEED=0

# =============================================================================
# Optional: Observability
# =============================================================================

# Logfire write token (optional - for observability)
# Get from https://logfire.pydantic.dev
LOGFIRE_TOKEN=your_logfire_token_here
